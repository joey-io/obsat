# Security

Do not open a public issue for a credential leak or security vulnerability.

Report security problems privately through GitHub's private vulnerability reporting feature for this repository.

Obsat does not operate a hosted service and does not collect credentials. Provider credentials are read from the user's environment and sent only to the provider adapter that requires them.

Never commit real API keys, tokens, `.env` files, or downloaded private data.
